{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task",
  "description": "Schema for task documents in Firestore",
  "type": "object",
  "properties": {
    "title": {
      "description": "The title of the task",
      "type": "string"
    },
    "details": {
      "description": "Additional details or description of the task",
      "type": "string"
    },
    "completed": {
      "description": "Whether the task has been completed",
      "type": "boolean"
    },
    "user": {
      "description": "Reference to the user document who created or owns the task",
      "type": "string"
    },
    "created": {
      "description": "The time when the task was created",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["title", "completed", "user", "created"]
}
